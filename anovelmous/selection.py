import nltk


def select_new_story_token(word_counts):
    """
    Will select the most popular, grammatically correct vote during the last 10 seconds.
    :param word_counts: list of dicts containing words and their counts
    :return: a story token object
    """
    new_story_token = None

    return new_story_token