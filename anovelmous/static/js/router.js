Anovelmous.Router.map(function() {
   this.resource('read', {'path': '/'});
});

/*Anovelmous.NovelRoute = Ember.Route.extend({
   model: function() {
      return this.store.find('novel');
   }
});*/